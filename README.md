# How to use this template
This template provides a minimal setup to create an intranet module.

First thing's first:
1. Clone this repository to your local environment
2. Make sure to add the remote origin of your new repository so you don't overwrite this code!
3. Run the command "npm install" on a terminal in Visual Studio Code
